// import { Readable, readable, writable } from "svelte/store";
// import API, { COMServer, User } from "./API";

// export const servers = readable([] as COMServer[], function(set) {
//     API.fetchServersList().then(data => set(data));
//     return () => {};
// })

// export const users = writable([] as User[], function(set) {
//     API.fetchUsersList().then(data => set(data));
//     return () => {}
// })